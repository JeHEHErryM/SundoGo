// Kept for the existing passenger imports; delegates to the shared map module.
export { default } from "@/components/map/Map";
export type { MapProps, MapPoint, LatLng } from "@/components/map/MapPlaceholder";
