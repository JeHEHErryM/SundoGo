{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "npx turbo build --filter=@sundogo/driver...",
  "outputDirectory": "apps/driver/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
