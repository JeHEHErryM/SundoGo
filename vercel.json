{
  "_comment": "Root vercel.json is superseded by per-app configs in apps/passenger/vercel.json, apps/driver/vercel.json, and apps/admin/vercel.json. Each frontend app is deployed as a separate Vercel project with its own configuration."
}
